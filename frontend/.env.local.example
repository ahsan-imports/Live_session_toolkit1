# Copy this file to .env.local

# Where the FastAPI backend is running
NEXT_PUBLIC_API_URL=http://localhost:8000

# Base URL shown/encoded on the "on air" panel for participants to join.
# Points at the minimal test participant page included in this scaffold.
NEXT_PUBLIC_PARTICIPANT_URL=http://localhost:3000/join
